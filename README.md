# Sentinel Scan

## Project Overview

The application will consist of:

- Flask Backend: Handles routing and API requests to AbuseIPDB.

- Environment Management: Uses python-dotenv to keep your API key safe.

- Package Management: Managed entirely by uv.

- Frontend: A simple HTML form to submit single or multiple IPs.

